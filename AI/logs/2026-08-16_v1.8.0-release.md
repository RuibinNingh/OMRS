# v1.8.0 发布记录

## 版本

- `omrs/version.py`：`v1.7.0` → `v1.8.0`
- 根 README、AI 项目摘要与版本表已同步。

## 行为变更

- 题目页与 A4/屏幕导出现在会把跨行 `$$...$$` 保持为完整的 display-math 块，`cases` 等多行公式不再被拆成普通文本。
- 设置页重启在 systemd 环境下交给 `systemctl restart --no-block`，手工启动仍保留原有回退路径。
- 增加可复用的 `deploy/omrs.service` 模板，包含 `OMRS_SYSTEMD_SERVICE` 标识。

## 验证

- Python 编译检查通过。
- Node 语法检查通过。
- OMRS 单元测试 15/15 通过。
- 跨行公式、A4/屏幕导出与设置页重启回归均已在本地完成验证。

## 发布边界

- 个人题库、Ledger、报告、运行日志、备份与本机部署元数据不纳入公开仓库。
- AI 凭据、令牌、Cookie 和连接密钥不写入仓库。
