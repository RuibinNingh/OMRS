# 2026-09-13 生产部署 v1.21.0（架构改造 P6 未完成状态）

## 任务

按用户要求，先停止 OMRS 生产，将未完成的架构改造第 6 期源码包替换到生产，并保留可回滚备份，供后续 Codex 继续修改。

本次部署不是“完成 P6”：包内交接日志明确说明 `assets/dist/` 为空，报告页、目录页、数据复盘页仍显示“尚未构建”兜底卡片；Vite 编译和 Vitest 仍待在有出站网络的环境补做。

## 包与基线

- 原始包：`/root/.hermes/cache/documents/doc_cf2d2cfa88cb_OMRS-rearch-P6.zip`
- 包形态：完整脱敏源码树 `OMRS/`，含 `SOURCE_EXPORT_MANIFEST.txt` 与 P6 交接日志；不含题库数据、`.git` 和生产历史日志。
- 包内版本：`omrs/version.py` = `v1.21.0`。
- 部署前生产：`v1.18.1`，161 道题，`conflict_count=0`。
- 部署前工作树有 12 条未提交状态（含 v1.18.1 文档、反馈弹窗改动等），未做 reset 或无范围 restore。

## 停机与备份

- 已执行 `systemctl stop omrs.service`、`systemctl reset-failed omrs.service`。
- 停机时 8471 监听已释放，无残留 OMRS 进程。
- 回滚目录：`/root/workspace/apps/releases/OMRS-v1.21.0-rearch-P6-rollback-20260913-115734/`
- 备份内容：停机前完整生产树（含 `.git`、`错题/`）、原始 zip、`pre-deploy-git-status.txt`、`uncommitted-worktree.patch`、`MANIFEST.sha256`。
- 备份清单：1184 个文件，`sha256sum -c` 通过，0 个失败；生产树约 54 MB。

## 部署范围

- 按包内文件逐项复制/更新，共 140 个包内文件发生复制或更新。
- 合并包内 `AI/logs/log.md` 的 P1–P6 / source-export 索引行，没有覆盖生产历史索引。
- 按 P5/P6 迁移清单删除：`assets/catalog.js`、`assets/data.js`、`assets/reports.js`、`tests/smoke_frontend_actions_catalog.js`。
- 未部署 `SOURCE_EXPORT_MANIFEST.txt`。
- 未覆盖生产侧历史 `AI/logs/`、`logs/`、`Task/`、`tool/`、`DEPLOYMENT_SOURCE.json`、旧导出文件。
- 未触碰 `错题/` 数据目录。

## 部署后核验

- 包树门禁：Python `unittest` **144/144** 通过；Node `tests/*.js` **93/93** 通过；`node tests/check_globals.js` 通过；`python3 tests/test_style_tokens.py` **7/7** 通过。
- 包树 `python3 tests/check_docs.py` 曾因脱敏包漏带生产已有的 `tests/smoke_feedback_result_modal.js` 报 1 条；该文件保留在生产后，生产树检查 **0 问题**。
- 生产树门禁：Python `unittest` **144/144** 通过；Node `tests/*.js` **94/94** 通过；`python3 tests/check_docs.py` **0 问题**。
- 包与生产源码逐文件核对：无包文件缺失、无内容差异；生产侧仅保留包明确排除的历史日志、任务、工具、缓存等文件。
- `错题/` 与停机前备份逐文件核对：330 个文件，新增 0、删除 0、变化 0。
- 版本文件和侧栏均为 `v1.21.0`。

## 启动与 Live 验收

- 已执行 `systemctl start omrs.service`，当前 `active (running)`，`NRestarts=0`，主进程 PID 3081396。
- 8471 已监听 `0.0.0.0:8471`。
- `/api/auth/status` 返回 `status=ok`、`auth_enabled=true`、`pin_configured=false`、`can_setup=true`、`tls=false`。
- 未认证访问 `/` 返回 401 自包含登录页；浏览器实测标题为 `🐴 OMRS — 登录`，显示“首次使用：设置 PIN”，登录表单存在。
- 未认证访问 `/assets/core.js` 返回 401，说明鉴权闸门仍生效。
- 本次没有设置 PIN、没有登录、没有执行任何写接口；未修改 `错题/.omrs/config.json`。
- 由于当前尚未配置 PIN，无法进入需认证的主应用页面；未把登录页验收冒充成三页功能验收。

## 启动后数据复核

- 启动后再次比较 `错题/`：文件数仍为 330，新增 0、删除 0；唯一字节变化是 `错题/.omrs/ledger.db`。
- 两份 SQLite 均通过 `PRAGMA integrity_check = ok`；`commits`、题目/掌握度/Session 等业务表行内容完全一致。
- 差异仅在 `workspace_fingerprint.last_seen_at` 与 `workspace_scan_status.last_scan_at`：服务启动扫描会刷新扫描时间，这是运行元数据更新，不是题库或学习记录写入。
- 本次没有执行 POST/PUT/PATCH/DELETE 写接口；该启动元数据变化保留在生产，不从运行中的数据库回拷备份覆盖。

## 未完成与交接给 Codex

1. 在有出站网络的机器执行 `cd web && npm install && npm run build && npm run test`。
2. 将 `assets/dist/` 实际构建产物带回生产/仓库后，重新验收报告、目录、数据复盘三页。
3. 用真实浏览器完成深浅色 × 密度组合、目录树、数据图表、报告页主路径验收。
4. 在真实证书环境补 TLS 的 `https://` 可访问与 `http://` 明确失败验收。
5. 生产尚未提交 Git；后续 Codex 应在当前 v1.21.0 未提交工作树上继续，不要重置或覆盖用户既有改动。

## 回滚

停止服务后，从回滚目录中的 `production-tree/` 按文件恢复生产源码与文档；恢复前后都不得触碰当前 `错题/` 数据。恢复后启动 `omrs.service`，用 `/api/auth/status` 与认证后的 `/api/status` 核对版本回到 `v1.18.1`。备份目录的 `MANIFEST.sha256` 可用于完整性复核。
