# 2026-09-06 · 生产部署 v1.15.0（UI 改版：密度层 + 首页重构 + 整屏工作台）

## 任务
部署 UI 升级包 `OMRS-ui-v1.15.0.zip`（v1.14.2 → **v1.15.0**）：界面密度（紧凑/舒适）、首页重构（今天 hero + 概览条 + 薄弱科目）、整屏工作台（questions/feedback/create/board/instant 五页）。纯前端 + 文档改动，后端 Python 除 `omrs/version.py` 外零改动，`/api/*` 无变化。

## 部署前状态
- 服务 active，`/api/status` = v1.14.2，question_count=154，conflict_count=0。
- 生产 git 树干净（无未提交改动，HEAD 6788b00）。
- 包形态：外层 zip = 内层源码导出 zip + `omrs-ui-v1.15.0.diff`（plain unified diff，`src/OMRS`→`work/OMRS` 前缀）+ 6 张验收截图。包内含任务日志 `AI/logs/2026-09-06_ui-density-home-workbench.md`（即交接文档）。
- 基线核验：`diff -rq 包树 生产树` 差异 = 包声明 11 文件 + 新任务日志 + `AI/logs/log.md`（含合并提示），无意外差异；包基于当前生产树导出。
- 补丁试打：`git apply --check -p2 --exclude='AI/logs/*'` 全过（log.md 单独处理）。

## 备份
`/root/workspace/apps/releases/OMRS-v1.15.0-rollback-20260906-102041/`
含：源码快照（cp -a）、`uncommitted-worktree-v1.14.2.patch`（空，git 树干净）、`pre-deploy-git-status.txt`、原始上传 zip + 内层 zip + diff、MANIFEST.sha256（sha256sum -c 0 失败）。

## 部署步骤
1. `git apply --verbose -p2 --exclude='AI/logs/*'` 应用 10 个文件（AI/README.md、AI/api.md、AI/frontend.md、README.md、assets/actions.js、assets/app.js、assets/dashboard.js、assets/styles.css、omrs/version.py、omrs_dashboard.html），全部 cleanly。
2. 逐文件 `cmp` 与包内副本 → 全部 IDENTICAL。
3. 复制新任务日志 `AI/logs/2026-09-06_ui-density-home-workbench.md`；`AI/logs/log.md` 按包内提示仅追加索引行（先查重，未覆盖历史索引）。
4. 不部署：`SOURCE_EXPORT_MANIFEST.txt`（导出元数据）。
5. 清 `__pycache__`。

## 门禁
- 包树：python unittest 59/59 OK；node --test 37/37 pass。
- 生产树（部署后）：python unittest 59/59 OK；node --test 37/37 pass。

## 重启与健康
- `systemctl restart` 遇 bind 竞态（Address already in use ×3，旧进程未及时退出），端口释放后 stop→start 成功。
- `/api/status`：status=ok，**version=v1.15.0**，question_count=154（不变），conflict_count=0。

## Live 浏览器验收（1440×950）
- 首页：`#dash-today` hero（142 道待复习 / 逾期 54 / 今日到期 88 / 已练 0·建议 20）、`.kpi-strip` 替代 5 张 stat-card（原 id 保留）、`#dash-weak` 薄弱科目（物理/化学/数学，点击跳题库筛选）、图表迁移（chart-trend/chart-labels 在 panel-data；chart-subjects/alerts/mastery/diff 容器已删）、行动推荐 4 条。侧栏版本 `v1.15.0 · 本地服务`。
- 工作台五页（questions/feedback/create/board/instant）：`.content.is-workbench` 生效，页面级滚动消失（950=950），仅内容区滚动。
- 题库：`#qb-counts` 已并入 `.qb-bar`；`#q-table-wrap thead th` position:sticky。
- 设置页：密度开关「紧凑/舒适」在位，默认紧凑；`setDensity('comfortable'|'compact')` 双轨（data-density attr + localStorage 'omrs-density'）同步正确。
- 全流程 console.error = 0。

## 未做的事
- 未提交 git（生产保持已提交状态，HEAD 6788b00 不动；工作树新增改动留给用户决定提交时机）。
- 密度仅两档、技术债条目未更新——与包内任务日志「待办」一致。

## 回滚
停服务 → 从 `releases/OMRS-v1.15.0-rollback-20260906-102041/` 恢复源码（或对 10 个文件 `git checkout -- <files>`，新任务日志与 log.md 索引行反向操作）→ 重启。`错题/` 未触碰。
