# v1.16.1 生产部署实录（2026-09-06 13:32）

## 任务
部署 OMRS v1.16.1 升级包（练习记录改读 Ledger、全站去原生弹窗、文档整理与写法规则）。

## 部署前状态
- 生产版本：v1.16.0（git HEAD e093ed7 "docs: sync current implementation facts"）
- 服务状态：active，TCP 8471，题库 154 题
- 工作树：干净（未提交改动已在 e093ed7 提交）

## 升级包内容
- 外层 zip：`OMRS-v1.16.1-full.zip`，直接解出 `OMRS/` 完整源码树
- 无内层 zip、无独立交接文档、无 diff
- 交接依据：包内 `CHANGED_FILES.txt` + `AI/logs/2026-09-06_ledger-records-docs-audit.md` 任务日志
- 版本号：`omrs/version.py` 显示 `v1.16.1`，属真版本升级

## 包基线核验
- 包树 171 文件，生产树 234 文件（差异主要是生产独有的历史日志、运行数据、工具）
- 包新增：`AI/changelog.md`、`AI/omr-import.md`、`AI/logs/2026-09-06_ledger-records-docs-audit.md`、`tests/check_docs.py`、`tests/test_question_records.py`、`CHANGED_FILES.txt`
- 包改动：27 个文件（13 个 JS、1 个 HTML、1 个 Python、6 个文档、2 个测试、4 个元数据）
- 差异符合预期，无意外文件冲突

## 备份
路径：`/root/workspace/apps/releases/OMRS-v1.16.1-rollback-20260906-133231/`
内容：
- `source/`：完整源码快照（cp -a）
- `uncommitted-worktree-v1.16.0.patch`：空（生产树干净）
- `pre-deploy-git-status.txt`：空（生产树干净）
- `OMRS-v1.16.1-full.zip`：原始升级包
- `MANIFEST.sha256`：231 文件，sha256sum -c 全部 OK

## 部署步骤
1. 解压包到 `/tmp/omrs-upgrade-1.16.1-JTvVcF/OMRS`
2. 包树门禁：
   - `python3 tests/check_docs.py` → 14 个文档 0 处问题
   - `for f in assets/*.js; do node --check "$f"; done` → 全部通过
   - `python3 -m unittest discover -s tests -p 'test_*.py'` → 61/61 OK（3 个 ResourceWarning 属测试代码未关闭文件句柄，非功能问题）
   - `for f in tests/test_*.js; do node --test "$f"; done` → 50 cases pass
3. 逐文件 cp 部署（排除 `CHANGED_FILES.txt`、`SOURCE_EXPORT_MANIFEST.txt`、`AI/logs/log.md`、`__pycache__`）
4. 手动 patch `AI/logs/log.md` 索引行（已追加）
5. 清理 `omrs/__pycache__` 与 `tests/__pycache__`
6. 生产树门禁：
   - `python3 tests/check_docs.py` → 14 个文档 0 处问题
   - JS 语法检查 → 全部通过
   - `python3 -m unittest` → 61/61 OK
   - `node --test tests/*.js` → 50 cases pass

## 重启与健康检查
- 第一次 `systemctl restart`：遇到 bind 竞态，Address already in use，auto-restart 死循环（NRestarts 达 3）
- 干净重启：`systemctl stop && sleep 2 && systemctl start` → 成功
- `/api/status`：
  - `version`: **v1.16.1** ✓
  - `status`: ok
  - `question_count`: 154（不变）
  - `workspace_scan.conflict_count`: 0
  - started_at: 2026-09-06T05:43:21+00:00

## Live 验收
按用户指示跳过浏览器验收（仅完成备份、双端门禁、重启 + 健康检查、部署日志）。

## 未做的事
- 浏览器实测：录反馈后详情记录刷新、`uiConfirm` 各处按钮文案、画廊战绩带 Ledger 数据源
- smoke 测试：`smoke_board_print.py` 在包树和生产树均失败（`test_full_then_incremental_print`），属既有问题，已记入 `AI/optimization.md`

## 回滚方式
```bash
systemctl stop omrs.service
cd /root/workspace/apps/releases/OMRS-v1.16.1-rollback-20260906-133231
cp -a source/* /root/workspace/apps/OMRS/
# 工作树干净，无需 patch 恢复
cd /root/workspace/apps/OMRS
rm -rf omrs/__pycache__ tests/__pycache__
systemctl start omrs.service
curl http://localhost:8471/api/status  # 确认版本回退到 v1.16.0
```

## 备注
- 用户明确要求"迅速，不需要验收"，解释为跳过浏览器实测，但备份、门禁、健康检查全部执行
- 生产保持未提交工作树（惯例），部署完成后工作区有 27 个 M 文件 + 5 个 ?? 文件
- 索引行已追加到 `AI/logs/log.md`
- 重启竞态是 v1.15.0 实录已知坑，skill 已更新 stop→sleep→start 标准流程
