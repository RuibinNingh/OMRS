# 2026-09-04 生产部署：v1.14.0 展示板/标记/题库 rework（含「仅打印新增」）

## 任务

上传升级包 `files (16).zip`（含 `OMRS-v1.14.0-board-rework.zip` 完整包 + `OMRS-changed-files-only.zip` 增量包 + 4 张验收截图），
将生产从 v1.14.0 Codex 初版升级到 v1.14.0 rework。生产此前运行的是 c52654d（v1.14.0 已提交、工作树干净），
初版存在展示板页永远停在「加载展示板…」、中文板名导出 500、导出模板缺 KaTeX/表格/分页、v1.14.0 新 CSS 全缺、
prompt/alert/confirm 贯穿、标记管理只能 prompt 等问题；本包是对同一版本号的 rework 修复 + 增量打印系统。

## 部署前状态

- 生产：`/root/workspace/apps/OMRS`，`omrs.service` active，`/api/status` → v1.14.0、147 题、conflict_count=0，
  git 工作树干净（HEAD c52654d）。
- 包树：`omrs/version.py` 同为 v1.14.0；`diff -rq 包树 生产树` 差异恰为增量包 27 文件（24 改 + 2 新测试 + 1 新日志），
  无意外差异；两内层包同 27 文件字节一致。
- 包树门禁：`python3 -m unittest discover -s tests` 53/53；`node --test tests/*.js` 27/27。

## 备份

`/root/workspace/apps/releases/OMRS-v1.14.0-board-rework-rollback-20260904-220159/`（6.3M）
源码快照（cp -a：omrs/ assets/ AI/ tests/ Skills/ deploy/ 根文件）+ uncommitted patch（空，树干净）+
pre-deploy git status + 原始 zip×2 + 4 张验收截图 + MANIFEST.sha256（`sha256sum -c` 0 失败）。

## 部署步骤

1. 按增量包清单逐文件 `cp` 27 个文件；部署后 `cmp` 逐文件核验全部一致。
2. 不部署：包内 `Task/`、`SOURCE_EXPORT_MANIFEST.txt`（完整包中未含）。
3. `AI/logs/log.md` 顶部追加本日志索引行。
4. 清 `__pycache__`。

## 生产树门禁

- `python3 -m unittest discover -s tests`：53/53 通过。
- `node --test tests/*.js`：27/27 通过。

## 重启 + 健康检查

`systemctl restart omrs.service` → active；`/api/status` → status=ok、v1.14.0、question_count=147 不变、
workspace_scan.conflict_count=0。

## Live 验收（浏览器 + API 实测）

- 展示板页（`switchTab('board')`）：三栏布局正常加载（板列表「考前必看」×2、题目列表、版面设置、
  打印模式、纸面记录区），**不再卡「加载展示板…」**；console.error 计数 0。
- 题库页（`switchTab('questions')`）：新壳生效——筛选、排序、表格/画廊、列/密度、快捷 chips
  （逾期 59/待攻克 147/顽固题 0/停用 0）、每行 ⋯ 菜单；console.error 计数 0。
- 标记 picker：行内「＋ 标记」→ picker 弹层（键盘提示、管理入口）→「管理标记…」弹层
  （名称/颜色/调度加成表单 + YAML 存储说明）均正常；console.error 计数 0。
- core.js 新 UI 原语 `uiToast/uiDialog/uiPrompt/uiConfirm` 全部为 function。
- 中文板名导出（隔离测试）：临时板「部署验收·中文板名测试」加 2 题 → `POST /api/export format=board` →
  HTTP 200，`Content-Disposition: filename="OMRS-BD--.html"; filename*=UTF-8''OMRS-BD-%E9%83%A8…%E6%B5%8B%E8%AF%95-%E9%94%99%E9%A2%98%E9%9B%86.html`。
  **初版 500 已修复**。
- 增量打印链路（同一临时板）：`POST /api/board/printed` 记录纸面（pages=2、cursor page 2/y 712.95、2 items）→
  加第 3 题 → `POST /api/export mode=new` → HTTP 200，`omrs-board-mode=new`，HTML 含 cursor 占位块说明
  （「cursor 之前的区域用占位块顶开，新题接在原纸空白处」）；无纸面记录时 mode=new 正确返回 400
  「请先「打印全部」并标记为已打印」。
- 测试板已删除，`/api/boards` 恢复「考前必看」×2。

## 未做的事

- 未提交 git（生产保持未提交工作树惯例，提交时机由用户决定）。
- 未跑 `tests/smoke_board_print.py`（Playwright 冒烟，测试目录门禁已覆盖其外其余 53+27 项；
  包内日志称其在无头 Chromium 下通过）。
- 未动 `错题/`、systemd 单元、`omrs_engine.py`。

## 回滚方式

`systemctl stop omrs.service` → 从
`/root/workspace/apps/releases/OMRS-v1.14.0-board-rework-rollback-20260904-220159/`
恢复源码目录（cp -a 覆盖回去）→ 重启。`错题/` 未动过无需恢复。
