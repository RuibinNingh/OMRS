# 2026-09-05 v1.14.1 生产部署

## 变更摘要

将上传的 `OMRS-v1.14.1.zip` 部署到 `/root/workspace/apps/OMRS`，修复展示板“打印预览”在导出请求 `await` 后才调用 `window.open`、容易被浏览器拦截的问题。

修复后的流程是：用户点击时同步创建空白预览窗口并写入占位提示，异步导出完成后用 `preview.location.replace(blobURL)` 填入内容；导出失败时关闭占位窗口。窗口对象保持不变，因此“已打印，记录纸面”的 `postMessage` 回传登记逻辑不受影响。

## 部署前状态

- 生产版本：`v1.14.0`
- `/api/status`：`status=ok`，题目数 `147`，`conflict_count=0`
- 生产工作树原有多项未提交改动；未覆盖或清理，完整状态与 patch 已保存到回滚包。
- 上传包 SHA-256：`a67979a40984e84815183c508905a9470944fa4000e30a68b254304d4437eb0d`
- 包内版本：`v1.14.1`；包内源码导出清单声明 158 个 Git 跟踪文件。

## 备份与部署

回滚包：`/root/workspace/apps/releases/OMRS-v1.14.1-rollback-20260905-125555/`

备份包含生产源码快照、部署前 `git status` / `git diff --name-status`、未提交 patch、原始升级包、部署前 API 与 systemd 状态，以及 `MANIFEST.sha256`。备份执行后已运行 `sha256sum -c MANIFEST.sha256`，全部通过。

差异预检确认只部署以下 6 个文件；未部署 `SOURCE_EXPORT_MANIFEST.txt`，未触碰 `Task/`、`错题/`、`omrs_engine.py` 或 systemd 单元：

- `assets/board.js`
- `omrs/version.py`
- `AI/README.md`
- `AI/api.md`
- `AI/frontend.md`
- `README.md`

## 验证

### 升级包门禁

- `python3 -m unittest discover -s tests`：53/53 通过
- `node --test tests/*.js`：24/24 通过

### 生产树门禁

- `python3 -m unittest discover -s tests`：53/53 通过
- `node --test tests/*.js`：27/27 通过
- 测试过程出现 `test_inbox.py` 原有 `ResourceWarning`（临时打开的 `annotations.jsonl` 未显式关闭），不影响通过结果。

### 重启与 API

- `systemctl restart omrs.service` 成功，服务 active
- `/api/status`：`status=ok`、`version=v1.14.1`、`question_count=147`、`workspace_scan.conflict_count=0`
- `/m`：HTTP 200，`text/html`，3324 bytes
- `GET /api/board?id=BD-20260904-4f51af`：HTTP 200；板仍为 3 题、已印 2 题 / 1 页、新增 1 题未打印

### 浏览器实机

在 `http://localhost:8471/` 根入口加载页面，页面显示 `v1.14.1 · 本地服务`，仪表盘显示总题数 147。

切换“展示板”后，对真实“打印预览”按钮执行可见坐标点击：

- 当前页保持在 OMRS 展示板页，没有被误导航；
- `BOARD_WINDOWS.size=1`；
- 新窗口为 `blob:http://localhost:8471/...`，标题为“错题集”；
- 预览页正文出现“OMRS · 错题集打印版”、`2 页（第 1–2 页）`及真实题目内容；
- 控制台错误 hook 收集到 0 条错误。

## 行为与兼容性

本次仅修复展示板前端预览窗口时序并更新版本/文档，无后端路由、题库、Ledger、板数据格式或迁移变化。已有纸面记录保持不变，预览动作不会自动标记为已打印。

未提交 Git；提交时机由用户决定。

## 回滚方式

停止 `omrs.service`，从上述回滚目录按备份快照恢复本次部署覆盖的源码文件，再启动服务并复核 `/api/status`。`错题/` 未被修改，无需恢复。
