# 2026-08-07 分类文件名与编号修复

## 变更摘要

- 修正上一轮迁移留下的文件名问题：分类目录内 19 道旧题从逗号前缀改为中文顿号前缀，分类索引保留不带逗号版本。
- 用户在迁移后新建的题目原为 `一元二次函数、方程与不等式1.md`（`OP-000132`）；为避免覆盖旧题 1，并遵循 `_next_uid()` 的最小可用编号规则，改为 `一元二次函数、方程与不等式12.md`。
- 所有题目的 `_omrs_id`、题干、答案、状态标签和复习历史保持不变；下一题将从当前最小可用编号 13 开始。

## Ledger 与历史兼容

- 工作区扫描追加 `CMT-000214` 至 `CMT-000233`，共 20 条 `question.move_external`，记录 20 道题的 UID/路径变更。
- 未新增、删除或改写任何 `review.*` 提交；旧 UID 作为历史提交中的当时名称继续保留。
- 分类目录中的重复逗号索引文件已删除；其内容与保留索引仅有换行符差异。

## 修改文件

- `错题/数学/一元二次函数、方程与不等式/`：20 道题和分类索引的文件名。
- `错题/.omrs/ledger.db`、`mastery_data.csv`：由工作区扫描同步。
- `AI/logs/log.md`：新增本次纠正索引。

## 验证

- 创建包含用户新题的补救前备份：`Task/OMRS-backup-20260807-before-filename-fix.zip`。
- 当前目录题目文件 20 个，逗号文件名 0 个，逗号分类索引不存在。
- 新分类投影 20 条，旧分类投影 0 条；新题 UID 为 `一元二次函数、方程与不等式12`。
- Ledger 校验：`valid=true`，233 条提交，链头 `CMT-000233`。
- 服务重启后工作区自检为 0 项变化、0 冲突。

## 同步过的文档

- `AI/logs/log.md`
