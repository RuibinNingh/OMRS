# 2026-09-02 生产部署 v1.11.0（升级包 files _11_.zip）

## 任务

用户上传 OMRS v1.11.0 升级包（`OMRS-v1.11.0-changed.zip` + `OMRS-v1.11.0.patch`），
指示「升级OMRS」。部署到生产 `/root/workspace/apps/OMRS`（systemd `omrs.service`，TCP 8471）。

## 部署前状态

- 生产版本：v1.10.0（工作树**未提交**，11 个文件的 v1.10 反馈工作台改动在位）
- `git log -3`：b3343bb / cfdd02d / 00c3a1d
- 基线确认：`git apply --check` 对 `OMRS-v1.11.0.patch` 干净适用——
  patch 的基线正是当前未提交的 v1.10 工作树，包内 11 个文件与本仓库兼容。

## 备份（回滚路径）

`/root/workspace/apps/releases/OMRS-v1.11.0-rollback-20260902-223014/`

- 源码快照：`omrs/`、`assets/`、`omrs_dashboard.html`、`README.md`、`AI/`、`tests/`、
  `omrs_engine.py`、`DEPLOYMENT_SOURCE.json`（215 文件 + `MANIFEST.sha256`）
- 未提交 v1.10 状态：`uncommitted-v1.10-worktree.patch`（998 行，`git diff` 全量）

## 部署步骤

1. 包树预检：`node --test tests/test_omr_import.js` → 16 pass / 0 fail。
   smoke 脚本需完整树（`assets/core.js` 等包外文件），留到生产树执行。
2. `git apply OMRS-v1.11.0.patch` 干净应用（884 insert / 22 delete，11 文件），
   三个核心文件与包内字节一致（diff 校验）。
3. 生产树门禁：`test_omr_import.js` 16/16、`smoke_feedback_omr_import.js` 全部通过、
   回归 `test_feedback_ui.js` + `test_question_suspend_frontend.js` 4/4、
   全部 JS 测试 22/22、`python3 -m unittest discover` 25/25。
4. `systemctl restart omrs.service`：首次 bind 冲突（旧进程 8471 未及释放，竞态），
   systemd 自动重启成功。PID 1389837，8471 监听，HTTP 200。

## Live 验收（浏览器实测）

- 版本：HTML 缓存串与运行时 `omrs.version` 均为 v1.11.0。
- 新函数全部就位：`fbImportText` / `fbPayloadKind` / `omrTemplateMode` /
  `omrExtractRecord` / `omrReadSheet` / `omrApplyToRows` / `fbReadClipboardAndFill` /
  `fbHandlePaste`；「📋 读剪贴板填写」按钮与「导入反馈」折叠面板渲染正常。
- **E2E（真实 Session EXP-20260831231147，40 题，未提交）**：
  导入 3 题 OMR JSON → 第 1 题 C/6 → `correct:true, score:6`；第 2 题 W/2 →
  `correct:false, score:2`；第 3 题多选 → `correct:null` + 备注「OMR：同一行涂了多个框」
  （不猜，留给人工）。状态行提示「自动填写 2 题（对 1 · 错 1）」。
- **无 Session 守卫**：未选 Session 导入被拒，提示先选 Session，`fbRows` 不变。
- **画廊预览修复**：导出画廊 144 卡渲染，`.gallery-preview .qv` 计算样式
  `white-space: normal`（修复前 pre-wrap 导致 ~220px 空白）。
- 控制台零错误；Session 反馈计数保持 0（验收未产生任何落库）。

## 未做的事

- 未提交 Git（保持工作树与 v1.10 同样的未提交状态，由用户决定提交时机）。
- 未改 `错题/` 数据目录、后端 Python（除 version.py）、systemd 单元。

## 回滚方式

停止 `omrs.service`，从 `releases/OMRS-v1.11.0-rollback-20260902-223014/` 恢复源码
目录（或 `git apply -R` 本 patch），重启即可；`错题/` 未动无需恢复。
