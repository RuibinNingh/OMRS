# 2026-09-06 · 生产部署 v1.16.0（题库练习记录：战绩带 + 记录模块）

## 任务
部署源码导出包 `OMRS-v1_16_0-practice-record.zip`（v1.15.0 → **v1.16.0**）：题库练习记录三层披露——①画廊卡脚注战绩带（默认开，`QB_STREAK`）；②题目详情记录模块（连错提示 / 四派生数 / 分数走势 sparkline / 明细折叠）；③全库聚合不做、表格视图不带战绩带（不额外拉详情）。纯前端 + 文档改动，后端 Python 仅 `omrs/version.py` 版本字符串，接口 / CSV / Ledger / Markdown 格式未动。

## 部署前状态
- 服务 active，`/api/status` = v1.15.0，question_count=154，conflict_count=0。
- 生产 git 工作树含上一轮 v1.15.0 部署的未提交改动（M 10 文件 + ?? 2 日志）——预期状态，v1.16 包基于该树导出。
- 包形态：外层 zip 直接解出 `OMRS/` 源码树（含 `SOURCE_EXPORT_MANIFEST.txt`），无独立交接文档；交接依据 = 包内任务日志 `AI/logs/2026-09-06_practice-record.md`。
- 基线核验：`diff -rq 包树 生产树` 差异 = 声明的 9 个改动文件 + `AI/logs/log.md` + 新任务日志 + 新测试 `tests/test_question_record_ui.js`，无意外差异。

## 备份
`/root/workspace/apps/releases/OMRS-v1.16.0-rollback-20260906-104746/`
含：源码快照（cp -a，含 v1.15 未提交改动）、`uncommitted-worktree-v1.15.0.patch`（git diff 全量）、`pre-deploy-git-status.txt`、原始上传 zip、MANIFEST.sha256（sha256sum -c 0 失败）。

## 部署步骤
1. 逐文件 `cp` 9 个改动文件（AI/frontend.md、README.md、assets/core.js、assets/qtable.js、assets/questions.js、assets/qview.js、assets/styles.css、omrs/version.py、omrs_dashboard.html）。
2. 复制新任务日志 `AI/logs/2026-09-06_practice-record.md` 与新测试 `tests/test_question_record_ui.js`。
3. 全部 `cmp` 包内副本 → 一致。
4. `AI/logs/log.md` 追加索引行（先查重，未覆盖历史）。
5. 不部署：`SOURCE_EXPORT_MANIFEST.txt`。
6. 清 `__pycache__`。

## 门禁
- 生产树：python unittest 59/59 OK；`node --test tests/*.js` **47/47 pass**（37 旧 + 10 新 test_question_record_ui.js）。
- 注：任务日志提到 `node --test tests/` 目录模式会因测试文件 global shim 互踩失败（已知问题，非本次引入）；本部署用多文件参数模式跑，全过。

## 重启与健康
- `systemctl restart` 一次成功（未遇 bind 竞态）。
- `/api/status`：status=ok，**version=v1.16.0**，question_count=154（不变），conflict_count=0。

## Live 验收
按用户指示**跳过浏览器验收**（仅完成门禁 + 健康检查）。

## 未做的事
- 未提交 git（生产惯例，工作树改动留给用户决定提交时机）。
- 测试隔离问题（`node --test tests/` 目录模式失败）未修，任务日志标注留待专门任务。

## 回滚
停服务 → 从 `releases/OMRS-v1.16.0-rollback-20260906-104746/` 恢复源码（cp -a）→ 重启。`错题/` 未触碰。
