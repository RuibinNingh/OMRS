# 2026-06-12 即时练习队列展示熟练度与到期时间

## 背景

「即时练习」主答题区已经显示熟练度和到期状态，但右侧「练习队列」只显示 UID 与来源，排队浏览时无法直接判断每题的熟练度或下次到期时间。

## 改动

- `assets/instant.js`：新增 `instQueueMeta()`，在队列按钮内展示 `熟练度 xx% · 到期 YYYY-MM-DD`，并复用 `formatDueInfo()` 显示逾期/今日到期/几天后等相对状态。
- `assets/styles.css`：队列项改为 UID + 次要信息的两行布局，保持右侧来源标签不变。
