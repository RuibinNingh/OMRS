# 2026-09-13 生产降级替换 v1.18.1

## 任务

用户要求取消当前 v1.21.0 架构改造升级，使用附件脱敏源码包替换生产，并明确接受保持外网监听的安全回退。

## 包与基线

- 原始包：`/root/.hermes/attachments/1789219376272_OMRS-source-sanitized-20260912T132249Z.zip`
- SHA-256：`b76f066b0abbc5294da08ec5694acb904f3838f43be09287cf8c2b67c5693964`
- 包形态：单层 `OMRS/` 脱敏源码树，174 个源码/测试/文档文件，CRC 校验通过。
- 包版本：`v1.18.1`。
- 部署前生产版本：`v1.21.0`，服务 active，题库 161 题。
- 部署前工作树含 v1.21.0 架构改造的大量未提交修改；未执行 reset 或无范围 restore。

## 备份与替换

- 回滚目录：`/root/workspace/apps/releases/OMRS-v1.18.1-downgrade-rollback-20260913-150914/`
- 已保存生产停机前完整树、上传原包、`pre-deploy-git-status.txt`、`uncommitted-worktree.patch`、HEAD 记录和 `MANIFEST.sha256`。
- 备份清单 4622 个文件，完整性校验 0 个失败。
- 已执行 `systemctl stop omrs.service`，确认 8471 释放后替换源码。
- 按包文件复制；不部署 `SOURCE_EXPORT_MANIFEST.txt`。
- 清理当前 v1.21.0 专属 `web/`、`assets/dist/`、`rearch.md`、鉴权/锁/资源缓存/异步 AI 模块及其测试；保留 `错题/`、`.git`、运行日志、任务材料和现有配置数据。
- `错题/` 未被附件包覆盖。

## 门禁与数据核验

- Python：`python3 -m unittest discover -s tests -p 'test_*.py'`，87/87 通过。
- Node：`node --test tests/*.js`，92/92 通过。
- 使用生产题库隔离副本预检：旧包可读取 161 题；Ledger 575 条提交，`verify_ledger` 通过。
- 生产题库替换前后均为 330 个文件，无新增、删除或题目内容变化。
- 当前 Ledger `PRAGMA integrity_check` 为 `ok`；业务表行数和内容一致，唯一变化是启动扫描更新的 `workspace_fingerprint` / `workspace_scan_status` 时间戳。

## Live 验收

- `omrs.service`：`active (running)`，`NRestarts=0`。
- 版本：`v1.18.1`。
- `/api/status`：HTTP 200，`question_count=161`，`conflict_count=0`。
- `/api/stats`：HTTP 200，总题数 161。
- `/`：HTTP 200，标题 `🐴 OMRS — 错题重构系统`，浏览器已进入主仪表盘。
- `/api/auth/status`：HTTP 404，符合旧版无鉴权实现。
- 监听：`0.0.0.0:8471`，符合用户确认的外网监听要求。
- TLS：未启用。

## 已知回退

本次是有意降级，不是 v1.21.0 的增量升级：

1. PIN 鉴权、失败限流和 TLS 代码被移除，当前配置中的鉴权字段由旧代码忽略。
2. 服务恢复为单线程 `TCPServer`，慢请求可能阻塞其它请求。
3. 内容指纹/ETag 缓存和 Vue/Vite 组件层被移除。
4. v1.21.0 架构改造的未提交源码只能从本次回滚目录恢复，不能依赖 Git HEAD。
5. 已知 `tests/check_docs.py` 初始有一条脱敏包自身的悬空引用（缺失 `tests/smoke_feedback_result_modal.js`）；本次仅做文档措辞修正，不影响运行功能。

## 回滚

停止 `omrs.service`，从回滚目录的 `production-tree/` 按文件恢复源码与文档，再启动服务；恢复过程中不得覆盖当前 `错题/`。如需恢复 v1.21.0 未提交工作树，以回滚目录中的 `uncommitted-worktree.patch` 和 `pre-deploy-git-status.txt` 为依据。
