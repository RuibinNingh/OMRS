# 2026-09-03 生产部署 v1.13.0（omrs2.zip / 收件箱 P3 源码导出）

## 任务

用户上传 `omrs2.zip`（外层含 `OMRS-v1.13.0-inbox-p3.zip` + `交接文档_2026-09-03-2.md`），
指示「部署一下生产(先备份)」。部署到生产 `/root/workspace/apps/OMRS`
（systemd `omrs.service`，TCP 8471）。

## 部署前状态

- 生产版本：v1.11.0（工作树未提交，v1.10 反馈工作台 + v1.11 OMR 导入改动在位）
- 包版本：v1.13.0 收件箱 P3（P1 录入流程 / P2 训练数据集 / P3 提供方·盲标·自动策略·清理）
- 基线核验：包内 137 个 git 跟踪文件与生产树逐一 diff——非收件箱文件两边
  **字节一致**，包正是基于当前 v1.11.0 未提交工作树的增量；无 `错题/`、
  无运行数据、无 `omrs_engine.py` / systemd 单元改动。

## 备份（回滚路径）

`/root/workspace/apps/releases/OMRS-v1.13.0-rollback-20260903-234234/`

- 源码快照：`omrs/`、`assets/`、`AI/`、`tests/`、`Skills/`、`deploy/`、根文档、
  `omrs_engine.py`、`DEPLOYMENT_SOURCE.json`（239 文件，`MANIFEST.sha256` 全部校验通过）
- 未提交状态：`uncommitted-worktree-v1.11.0.patch`（1399 行）+ `pre-deploy-git-status.txt`
- 原始上传包与交接文档同存于回滚目录

## 部署步骤

1. 包树预检（只读）：`python3 -m unittest discover` 39/39；`node --test tests/*.js` 全过。
2. 覆盖部署：收件箱新增文件（`omrs/inbox.py`、`assets/inbox.js`、
   `assets/inbox_mobile.html`、`AI/inbox.md`、`tests/test_inbox.py`、两份 inbox 日志）
   + 更新（`omrs/server.py`、`ai_assist.py`、`common.py`、`version.py`、
   `assets/app.js`、`styles.css`、`omrs_dashboard.html`、`README.md`、`AI/*.md`）。
   按惯例不部署包内 `Task/`、`SOURCE_EXPORT_MANIFEST.txt`。
3. `AI/logs/log.md` 合并包内两行索引（P1 / P3）。
4. 生产树门禁：Python 39/39、Node 17/17（含 v1.11 的 OMR 导入与反馈 UI 回归）。
5. `systemctl restart omrs.service`：一次成功，PID active，8471 监听。

## Live 验收（浏览器实测）

- `/api/status`：`version: v1.13.0`，144 题不变，workspace 扫描 0 冲突。
- 仪表盘 HTML 版本串 v1.13.0；「录入题目」页收件箱流渲染正常，
  「▦ 模板框选」按钮在位，`inboxInit` / `ibDetect` 已加载，待处理计数 0。
- `/api/inbox/items` 200、`/api/inbox/dataset/stats` 200（含 blind / storage 新统计）。
- 手机上传页 `/m` 200（text/html）。
- 控制台零错误。

## 未做的事

- 未提交 Git（与 v1.10 / v1.11 部署同惯例，提交时机由用户决定）。
- 未改 `错题/` 数据目录、`omrs_engine.py`、systemd 单元。
- 未接真实 VLM / 本地检测服务（交接文档 §3：接手后第一件事，需用户提供模型端点）。

## 回滚方式

停止 `omrs.service`，从 `releases/OMRS-v1.13.0-rollback-20260903-234234/`
恢复源码目录（或 `git apply -R` 部署 diff），重启即可；`错题/` 未动无需恢复。
