# 2026-09-08 生产部署 v1.17.0（展示板文件夹与统一选板浮层）

## 包与来源
- 外层 zip：`doc_234ee9ada44d_files (23).zip` → 内层 `OMRS-board-picker-folders-v1.17.0.zip`（直出 `OMRS/` 源码树 + `SOURCE_EXPORT_MANIFEST.txt` + 2 张主题预览截图）。
- 交接依据：包内任务日志 `AI/logs/2026-09-08_board-picker-folders.md`（P0 统一选板浮层 + P1 文件夹，boards.json v1→v2）。
- 生产基线：v1.16.1，未提交工作树。

## 部署步骤（按用户指示跳过浏览器验收，其余全做）
1. 备份：`releases/OMRS-v1.17.0-rollback-20260908-054742/`（prod-tree 快照 + uncommitted.patch + git-status + 原始包 + 交接任务日志 + MANIFEST.sha256，1037 文件 sha256 全过）。
2. 包树门禁：Python 67/67 OK；`node --test tests/*.js` 56/57——唯一失败 `smoke_frontend_actions_catalog.js` 为**既有基线问题**（日期相对型断言，生产 v1.16.1 同文件同样失败，该文件不在本次变更清单内）。
3. 树差异核验：仅本次功能文件（boards.py / server.py / version.py / 8 个前端文件 / omrs_dashboard.html / 4 份 AI 文档 / README / 2 个测试 / 任务日志），`diff -rq` 无意外差异；未部署 `Task/`、`SOURCE_EXPORT_MANIFEST.txt`。
4. 逐文件 `cp` 覆盖 + 新任务日志一并部署；部署后两树 diff 仅剩导出时排除项（changelog.md / omr-import.md / check_docs.py / test_question_records.py 等，预期）。
5. 生产树门禁：Python 69/69 OK；JS 56/57（同上基线失败）。
6. 重启：首次 `stop && sleep 1 && start` 遇 bind 竞态（旧进程未释放端口，auto-restart 循环）→ `stop` 后确认端口空闲、`reset-failed`，干净 `start` 成功。
7. 健康检查：`/api/status` version=v1.17.0、question_count=156、服务 active。
8. 后端新功能抽查（非浏览器验收）：`/api/boards` 已返回 `{boards, folders}`，board 含 `folder_id`/`order`/`uids`——v1 boards.json 自动迁移成功（folders 为空数组，未报错不丢板）。

## 收尾文档
- 任务日志 `AI/logs/2026-09-08_board-picker-folders.md`（包内原文）已部署。
- `AI/changelog.md` 顶部补 v1.17.0 段落。
- `AI/logs/log.md` 追加本部署索引行。

## 浏览器验收
按用户指示「不需要验收」跳过浏览器实测（浮层/文件夹树 UI 未做浏览器确认）；后端 API 与迁移路径已抽查通过。
