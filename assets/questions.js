// === assets/questions.js — 题目库表格/画廊视图 + 题目 Modal ===
const QB_TABLE_COLUMN_ORDER = ['select', 'main', 'labels', 'mastery', 'due', 'status', 'difficulty', 'decayed', 'attempts', 'last_review', 'ef', 'actions'];
const QB_TABLE_COLUMN_LABELS = {
  select: '<input type="checkbox" id="qb-select-all" onchange="qbSelectAll(this.checked)">',
  main: 'UID / 题目',
  labels: '标记',
  mastery: '熟练度',
  due: '到期',
  status: '状态',
  difficulty: '难度',
  decayed: '衰减后',
  attempts: '次数',
  last_review: '上次复习',
  ef: 'EF',
  actions: '操作',
};
function renderQuestionTable(items){qvSetContext('q',items.map(item=>item.uid));const tbody=document.getElementById('q-tbody');const columns=typeof qbVisibleColumns==='function'?qbVisibleColumns():new Set(QB_VISIBLE_COLUMNS||['select','main','labels','mastery','due','status','actions']);const cell=(key,html,tag='td')=>columns.has(key)?`<${tag} data-qb-col="${key}">${html}</${tag}>`:'';const head=tbody?.closest('table')?.querySelector('thead tr');if(head)head.innerHTML=QB_TABLE_COLUMN_ORDER.filter(key=>columns.has(key)).map(key=>cell(key,QB_TABLE_COLUMN_LABELS[key],'th')).join('');tbody.innerHTML=items.map(item=>{const masteryPct=`${(asNumber(item.mastery,0)*100).toFixed(0)}%`;const masteryColor=asNumber(item.mastery,0)>.8?'var(--green)':asNumber(item.mastery,0)>.4?'var(--yellow)':'var(--red)';const tagClass=(item.tag||'').includes('已击杀')?'kill':(item.tag||'').includes('易错')?'trap':'attack';const statusHtml=item.suspended?'<span class="tag suspended">停用</span>':`<span class="tag ${tagClass}">${escapeHtml((item.tag||'').replace(/#/g,''))}</span>`;const labels=lblChips(item.labels||[],{add:true});const meta=`<div class="q-row-meta">${escapeHtml(item.subject||'')} · ${escapeHtml(item.category||'')}</div>`;const dueHtml=formatDueInfo(getDueDays(item));const extra={difficulty:escapeHtml(item.difficulty??''),decayed:`${(asNumber(item.decayed_mastery,0)*100).toFixed(0)}%`,attempts:escapeHtml(item.attempts??0),last_review:escapeHtml(item.last_review||'—'),ef:escapeHtml(item.ef??'')};return`<tr class="${item.suspended?'q-row-suspended':''}${typeof QB_CURSOR_UID!=='undefined'&&QB_CURSOR_UID===item.uid?' qb-cursor':''}" aria-selected="${typeof QB_CURSOR_UID!=='undefined'&&QB_CURSOR_UID===item.uid?'true':'false'}" data-q-row="${escapeAttr(item.uid)}">${cell('select',`<input type="checkbox" class="qb-row-check" data-qb-uid="${escapeAttr(item.uid)}" ${QB_SELECTED.has(item.uid)?'checked':''}>`)}${cell('main',`<strong>${escapeHtml(item.uid)}</strong>${meta}`)}${cell('labels',`<span data-lbl-target="${escapeAttr(item.uid)}">${labels}</span>`)}${cell('mastery',`<span class="m-bar"><span class="m-bar-fill" style="width:${asNumber(item.mastery,0)*100}%;background:${masteryColor}"></span></span>${masteryPct}`)}${cell('due',dueHtml)}${cell('status',statusHtml)}${['difficulty','decayed','attempts','last_review','ef'].map(key=>cell(key,extra[key])).join('')}${cell('actions',`<div class="q-edit-wrap" data-edit-uid="${escapeAttr(item.uid)}"><button class="btn sm q-more" type="button" data-q-more="${escapeAttr(item.uid)}">⋯</button><div class="q-edit-menu"><button type="button" data-q-action="view" data-q-uid="${escapeAttr(item.uid)}">查看</button><button type="button" data-q-action="board" data-q-uid="${escapeAttr(item.uid)}">加入展示板</button><button type="button" data-q-action="labels" data-q-uid="${escapeAttr(item.uid)}">打标记</button><button type="button" data-q-action="edit" data-q-uid="${escapeAttr(item.uid)}">编辑 Markdown</button>${item.suspended?`<button type="button" data-q-action="resume" data-q-uid="${escapeAttr(item.uid)}">恢复题目</button>`:`<button type="button" data-q-action="suspend" data-q-uid="${escapeAttr(item.uid)}">停用题目</button>`}<button type="button" class="danger" data-q-action="delete" data-q-uid="${escapeAttr(item.uid)}">删除题目</button></div></div>`)}</tr>`}).join('');if(!items.length){tbody.innerHTML=`<tr><td colspan="${columns.size}" style="text-align:center;color:var(--fg3);padding:28px">暂无匹配题目</td></tr>`}qbRenderSummary(items,getFilterState('q'));qbRenderChips(getFilterState('q'));qbUpdateBatchBar();qbRenderDensity();if(typeof qbRenderControls==='function')qbRenderControls()}
function renderQuestionGallery(items){qvSetContext('q',items.map(item=>item.uid));const box=document.getElementById('q-gallery-wrap');if(!items.length){box.innerHTML='<div class="picker-box"><div class="empty-inline">暂无匹配题目</div></div>';return}box.innerHTML=`<div class="gallery-grid">${items.map(item=>{const tagClass=(item.tag||'').includes('已击杀')?'kill':(item.tag||'').includes('易错')?'trap':'attack';const detail=QUESTION_CACHE[item.uid];const previewHtml=detail?qvHtml(detail,item,QV_CARD_OPTS):'<div class="preview-placeholder">正在加载题目预览…</div>';const knowledgeTags=(item.knowledge_tags||[]).map(tag=>`<span class="tag" style="background:var(--accent-bg);color:var(--accent2)">${escapeHtml(tag)}</span>`).join('');const labels=lblChips(item.labels||[]);const masteryPct=(asNumber(item.mastery,0)*100).toFixed(0);const decayedPct=(asNumber(item.decayed_mastery,0)*100).toFixed(0);const masteryColor=asNumber(item.mastery,0)>.8?'var(--green)':asNumber(item.mastery,0)>.4?'var(--yellow)':'var(--red)';const dueDays=getDueDays(item);const dueText=dueDays===null?'':dueDays<0?`逾期${Math.abs(dueDays)}天`:dueDays===0?'今日到期':`${dueDays}天后到期`;const dueColor=dueDays===null?'var(--fg3)':dueDays<0?'var(--red)':dueDays===0?'var(--yellow)':dueDays<=3?'var(--yellow)':'var(--fg3)';const statusHtml=item.suspended?'<span class="tag suspended">停用</span>':`<span class="tag ${tagClass}">${escapeHtml((item.tag||'').replace(/#/g,''))}</span>`;return`<div class="gallery-card ${item.suspended?'q-card-suspended':''}" data-q-row="${escapeAttr(item.uid)}"><div class="gallery-head"><label class="qb-gallery-select"><input type="checkbox" class="qb-row-check" data-qb-uid="${escapeAttr(item.uid)}" ${QB_SELECTED.has(item.uid)?'checked':''}><span class="uid">${escapeHtml(item.uid)}</span></label>${statusHtml}</div><div class="q-label-cell" data-lbl-target="${escapeAttr(item.uid)}">${labels||'<span class="hint">暂无标记</span>'}</div><div class="question-gallery-meta">${escapeHtml(item.subject||'')} · ${escapeHtml(item.category||'')}<br>难度 ${escapeHtml(item.difficulty)} · 上次复习 ${escapeHtml(item.last_review||'—')}${dueText?` · <span style="color:${dueColor}">${dueText}</span>`:''}</div><div class="question-progress-row"><div class="question-progress-main"><span>熟练度</span><span class="m-bar"><span class="m-bar-fill" style="width:${masteryPct}%;background:${masteryColor}"></span></span><span>${masteryPct}%</span></div><span>衰减后 ${decayedPct}%</span></div><div class="tag-row">${knowledgeTags||'<span class="helper-inline">无额外知识点</span>'}</div><div class="gallery-preview question-gallery-preview" data-question-preview-uid="${escapeAttr(item.uid)}">${previewHtml}</div><div class="question-gallery-actions"><span class="helper-inline">尝试 ${escapeHtml(item.attempts)} 次</span><div class="q-action-row"><button class="btn sm" onclick="viewQ(${jsArg(item.uid)},'q')">查看详情</button>${renderQuestionEditMenu(item.uid)}</div></div></div>`}).join('')}</div>`}
// 画廊缩略卡也走 qview（bare + clamp），与 Modal / 反馈台共用同一套题面渲染
const QV_CARD_OPTS={layout:'stack',showMeta:false,showAnswer:false,showNotes:false,showHistory:false,actions:[],bare:true,clamp:6};
async function hydrateQuestionGalleryPreviews(){const nodes=[...document.querySelectorAll('.question-gallery-preview[data-question-preview-uid]')];await Promise.all(nodes.map(async node=>{const uid=node.dataset.questionPreviewUid;if(!uid)return;const detail=await ensureQuestionDetail(uid);if(node.dataset.questionPreviewUid===uid){node.innerHTML=qvHtml(detail,getItemByUid(uid)||{uid},QV_CARD_OPTS)}}))}
function setQView(view){Q_VIEW=view;renderQ()}
function renderQ(){const items=filterItems(getItems(),getFilterState('q'));const tableWrap=document.getElementById('q-table-wrap');const galleryWrap=document.getElementById('q-gallery-wrap');document.getElementById('q-view-table').classList.toggle('active',Q_VIEW==='table');document.getElementById('q-view-gallery').classList.toggle('active',Q_VIEW==='gallery');if(Q_VIEW==='gallery'){tableWrap.style.display='none';galleryWrap.style.display='block';renderQuestionGallery(items);hydrateQuestionGalleryPreviews()}else{tableWrap.style.display='block';galleryWrap.style.display='none';renderQuestionTable(items)}qbRenderSummary(items,getFilterState('q'));qbRenderChips(getFilterState('q'));qbUpdateBatchBar()}
function filterQ(){renderQ()}
function normalizeImageName(raw){return String(raw??'').trim().split('/').pop().split('\\').pop()}
function renderInlineImage(name,alt,width){const imageName=normalizeImageName(name);const capped=width?Math.min(asNumber(width,0),600):0;const style=`${capped?`max-width:${capped}px;`:'max-width:100%;'}height:auto;display:block;margin:8px 0;border-radius:4px`;const safeName=escapeHtml(imageName);return `<img src="/api/image?name=${encodeURIComponent(imageName)}" alt="${escapeAttr(alt||imageName)}" style="${style}" onerror="this.outerHTML='&lt;span style=&quot;color:var(--red);font-size:.8rem&quot;&gt;[图片缺失: ${safeName}]&lt;/span&gt;'">`}
function renderLatexSegment(source,display){const latex=String(source??'');if(window.katex&&typeof window.katex.renderToString==='function'){try{return window.katex.renderToString(latex,{displayMode:!!display,throwOnError:false,strict:'ignore',trust:false,output:'html'})}catch(error){}}return `<span class="math ${display?'display':''}">${escapeHtml(latex)}</span>`}
function renderMdInline(text=''){const source=String(text??'');const tokenRe=/(!\[\[[^\]]+?\]\]|!\[[^\]]*\]\([^)]+\)|\$\$[\s\S]+?\$\$|\$[^$\n]+\$)/g;let html='',last=0,match;while((match=tokenRe.exec(source))){html+=escapeHtml(source.slice(last,match.index));const token=match[0];let parsed=token.match(/^!\[\[([^\]|]+?)(?:\|(\d+))?\]\]$/);if(parsed){html+=renderInlineImage(parsed[1],parsed[1],parsed[2]||'');last=tokenRe.lastIndex;continue}parsed=token.match(/^!\[([^\]]*)\]\(([^)]+)\)$/);if(parsed){const imageName=normalizeImageName(parsed[2]);html+=renderInlineImage(imageName,parsed[1]||imageName,'');last=tokenRe.lastIndex;continue}if(token.startsWith('$$')&&token.endsWith('$$')){html+=renderLatexSegment(token.slice(2,-2),true)}else if(token.startsWith('$')&&token.endsWith('$')){html+=renderLatexSegment(token.slice(1,-1),false)}last=tokenRe.lastIndex}return html+escapeHtml(source.slice(last))}
function splitMdTableRow(line){const source=String(line??'').trim().replace(/^\|/,'').replace(/\|$/,'');const cells=[];let cell='';for(let index=0;index<source.length;index++){const ch=source[index];if(ch==='\\'&&source[index+1]==='|'){cell+='|';index++}else if(ch==='|'){cells.push(cell.trim());cell=''}else{cell+=ch}}cells.push(cell.trim());return cells}
function isMdTableSeparator(line){const cells=splitMdTableRow(line);return cells.length>0&&cells.every(cell=>/^:?-{3,}:?$/.test(cell))}
function renderMdTable(header,rows){const headings=splitMdTableRow(header);const body=rows.map(row=>{const cells=splitMdTableRow(row);return `<tr>${headings.map((_,index)=>`<td>${renderMdInline(cells[index]||'')}</td>`).join('')}</tr>`}).join('');return `<div class="md-table-wrap"><table class="md-table"><thead><tr>${headings.map(cell=>`<th>${renderMdInline(cell)}</th>`).join('')}</tr></thead><tbody>${body}</tbody></table></div>`}
function countDisplayMathDelimiters(line=''){const source=String(line??'');let count=0;for(let index=0;index<source.length-1;index++){if(source[index]==='$'&&source[index+1]==='$'&&(index===0||source[index-1]!=='\\')){count++;index++}}return count}
function findDisplayMathEnd(lines,start){let delimiters=0;for(let index=start;index<lines.length;index++){delimiters+=countDisplayMathDelimiters(lines[index]);if(delimiters&&delimiters%2===0)return index}return -1}
function renderMdContent(text=''){const lines=String(text??'').split(/\r?\n/);let html='';for(let index=0;index<lines.length;){if(index+1<lines.length&&lines[index].includes('|')&&isMdTableSeparator(lines[index+1])){const header=lines[index],rows=[];index+=2;while(index<lines.length&&lines[index].includes('|')&&lines[index].trim()){rows.push(lines[index]);index++}html+=renderMdTable(header,rows);continue}let source=lines[index],end=index;if(countDisplayMathDelimiters(source)%2===1){const close=findDisplayMathEnd(lines,index);if(close>=index){source=lines.slice(index,close+1).join(String.fromCharCode(10));end=close}}html+=renderMdInline(source);if(end<lines.length-1)html+='<br>';index=end+1}return html}
async function ensureQuestionDetail(uid){if(QUESTION_CACHE[uid])return QUESTION_CACHE[uid];if(QUESTION_PENDING[uid])return QUESTION_PENDING[uid];QUESTION_PENDING[uid]=(async()=>{try{const detail=await api(`/api/question?uid=${encodeURIComponent(uid)}`);if(detail?.error)throw new Error(detail.error);QUESTION_CACHE[uid]=detail;return detail}catch(error){const fallback=getItemByUid(uid)||{};QUESTION_CACHE[uid]={uid,subject:fallback.subject||'',category:fallback.category||'',difficulty:fallback.difficulty||'',question:'（无法加载题目预览）',notes:'',answer:'',history:'',tag:fallback.tag||'',knowledge_tags:fallback.knowledge_tags||[],_fallback:true};return QUESTION_CACHE[uid]}finally{delete QUESTION_PENDING[uid]}})();return QUESTION_PENDING[uid]}
// 题目 Modal：内容全部交给 qview 渲染（双栏 + 就地操作）。第二个参数是可选的翻页上下文，
// 可传 uid 数组，或传 qvSetContext() 登记过的上下文名（如 'q' / 'export'）；不传就退化成单题。
async function viewQ(uid,context){const list=Array.isArray(context)?context:(typeof context==='string'?qvContext(context):null);if(list&&list.length&&list.indexOf(uid)>=0)QV_NAV={list:[...list],index:list.indexOf(uid)};else if(QV_NAV.list.indexOf(uid)<0)QV_NAV={list:[uid],index:0};else QV_NAV.index=QV_NAV.list.indexOf(uid);document.getElementById('modal').classList.add('open');qvNavRender();await qvRender('#modal-stage',uid,{layout:'split',actions:['edit','board','labels','suspend','delete']})}
function closeModal(){const modal=document.getElementById('modal');if(modal)modal.classList.remove('open');const stage=document.getElementById('modal-stage');if(stage&&typeof QV_MOUNTS!=='undefined')QV_MOUNTS.delete(stage)}
function renderQuestionEditMenu(uid){const item=getItemByUid(uid)||{};const suspendAction=item.suspended?`<button onclick="resumeQuestion(${jsArg(uid)})">恢复题目</button>`:`<button onclick="suspendQuestion(${jsArg(uid)})">停用题目</button>`;return `<div class="q-edit-wrap" data-edit-uid="${escapeAttr(uid)}"><button class="btn sm" onclick="toggleQuestionEditMenu(event,${jsArg(uid)})">编辑</button><div class="q-edit-menu"><button onclick="moveQuestionPrompt(${jsArg(uid)})">迁移到其他分类</button><button onclick="openMarkdownEditor(${jsArg(uid)})">编辑 Markdown</button>${suspendAction}<button class="danger" onclick="deleteQuestion(${jsArg(uid)})">删除题目</button></div></div>`}
function toggleQuestionEditMenu(event,uid){event.stopPropagation();document.querySelectorAll('.q-edit-wrap.open').forEach(node=>{if(node.dataset.editUid!==uid)node.classList.remove('open')});const box=event.currentTarget.closest('.q-edit-wrap');if(box)box.classList.toggle('open')}
document.addEventListener('click',event=>{if(!event.target.closest?.('.q-edit-wrap'))document.querySelectorAll('.q-edit-wrap.open').forEach(node=>node.classList.remove('open'))})
document.addEventListener('click', event => {
  const more = event.target.closest?.('[data-q-more]');
  if (more) {
    event.stopPropagation();
    toggleQuestionEditMenu(event, more.dataset.qMore);
    return;
  }
  const action = event.target.closest?.('[data-q-action]');
  if (action) {
    event.stopPropagation();
    const uid = action.dataset.qUid;
    [...document.querySelectorAll('[data-edit-uid]')].find(node => node.dataset.editUid === uid)?.classList.remove('open');
    const kind = action.dataset.qAction;
    if (kind === 'view') viewQ(uid, 'q');
    else if (kind === 'board' && typeof boardQuickAdd === 'function') boardQuickAdd(uid);
    else if (kind === 'labels') openLabelPicker(uid, action);
    else if (kind === 'edit') openMarkdownEditor(uid);
    else if (kind === 'suspend') suspendQuestion(uid);
    else if (kind === 'resume') resumeQuestion(uid);
    else if (kind === 'delete') deleteQuestion(uid);
    return;
  }
  const row = event.target.closest?.('[data-q-row]');
  if (row && !event.target.closest('button,input,[data-lbl-target]')) viewQ(row.dataset.qRow, 'q');
});
document.addEventListener('change', event => {
  const check = event.target.closest?.('[data-qb-uid]');
  if (check) qbSelect(check.dataset.qbUid, check.checked);
});
async function moveQuestionPrompt(uid){const item=getItemByUid(uid)||{};const subject=prompt('目标科目',item.subject||'');if(subject===null)return;const category=prompt('目标分类',item.category||'');if(category===null||!category.trim())return;try{const result=await api('/api/question/move',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({uid,subject:subject.trim()||item.subject,category:category.trim()})});delete QUESTION_CACHE[uid];await reloadData();alert(`${uid} 已迁移为 ${result.uid}`)}catch(error){alert(`迁移失败: ${error.message}`)}}
async function suspendQuestion(uid){const confirmed=confirm(`确定停用题目「${uid}」？\n\n停用后不会进入复习调度、统计或数据分析；题目正文和历史记录会保留，可随时恢复。`);if(!confirmed)return;try{await api('/api/question/suspend',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({uid})});await reloadData();if(typeof qvInvalidate==='function')await qvInvalidate(uid);await loadHist();alert(`${uid} 已停用`)}catch(error){alert(`停用失败: ${error.message}`)}}
async function resumeQuestion(uid){const confirmed=confirm(`确定恢复题目「${uid}」？\n\n恢复后将重新参与复习调度、统计和数据分析。`);if(!confirmed)return;try{await api('/api/question/resume',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({uid})});await reloadData();if(typeof qvInvalidate==='function')await qvInvalidate(uid);await loadHist();alert(`${uid} 已恢复`)}catch(error){alert(`恢复失败: ${error.message}`)}}
async function deleteQuestion(uid){const confirmed=confirm(`确定删除题目「${uid}」？\n\n这会删除题目的 Markdown 正文，并在 Ledger 中追加归档记录。历史反馈仍会保留，但题目正文无法通过 Ledger 恢复；附件图片不会删除。`);if(!confirmed)return;try{await api('/api/question/delete',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({uid})});delete QUESTION_CACHE[uid];await reloadData();await loadHist();alert(`${uid} 已删除`)}catch(error){alert(`删除失败: ${error.message}`)}}
async function openMarkdownEditor(uid){try{const result=await api(`/api/question/raw?uid=${encodeURIComponent(uid)}`);document.getElementById('md-edit-uid').textContent=result.uid;document.getElementById('md-edit-path').textContent=result.file_path||'';document.getElementById('md-edit-text').value=result.markdown||'';document.getElementById('md-edit-status').textContent='';document.getElementById('md-editor').dataset.uid=result.uid;document.getElementById('md-editor').classList.add('open')}catch(error){alert(`无法打开 Markdown: ${error.message}`)}}
function closeMarkdownEditor(){document.getElementById('md-editor').classList.remove('open')}
async function saveMarkdownEditor(){const modal=document.getElementById('md-editor');const uid=modal.dataset.uid;const markdown=document.getElementById('md-edit-text').value;const status=document.getElementById('md-edit-status');status.textContent='保存中...';try{await api('/api/question/markdown',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({uid,markdown})});delete QUESTION_CACHE[uid];status.innerHTML='<span style="color:var(--green)">已保存</span>';await reloadData();if(typeof qvInvalidate==='function')await qvInvalidate(uid);setTimeout(closeMarkdownEditor,350)}catch(error){status.innerHTML=`<span style="color:var(--red)">${escapeHtml(error.message)}</span>`}}
